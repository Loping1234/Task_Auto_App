<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Team | TaskApp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/dark-theme.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        :root {
            --sidebar-width: 260px;
            --primary-color: #4f46e5;
            --secondary-color: #1e293b;
        }

        body {
            background-color: #f8fafc;
            font-family: 'Inter', sans-serif;
        }

        .sidebar {
            width: var(--sidebar-width);
            height: 100vh;
            position: fixed;
            left: 0;
            top: 0;
            background-color: var(--secondary-color);
            color: white;
            padding: 24px;
            z-index: 1000;
        }

        .sidebar-brand {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 32px;
            display: flex;
            align-items: center;
            gap: 12px;
            color: #fff;
            text-decoration: none;
        }

        .nav-link {
            color: #cbd5e1;
            padding: 12px 16px;
            border-radius: 8px;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 12px;
            transition: all 0.3s;
        }

        .nav-link:hover,
        .nav-link.active {
            background-color: rgba(255, 255, 255, 0.1);
            color: white;
        }

        .main-content {
            margin-left: var(--sidebar-width);
            padding: 40px;
        }

        .list-card {
            border: none;
            border-radius: 16px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            background: white;
        }

        .logout-btn {
            position: absolute;
            bottom: 24px;
            left: 24px;
            right: 24px;
        }
    </style>
</head>

<body>
    <div class="sidebar">
        <a href="/dashboard" class="sidebar-brand text-white">
            <i class="fas fa-shield-alt text-primary"></i>
            <span>Admin Panel</span>
        </a>
        <nav class="nav flex-column mt-4">
            <a href="/dashboard" class="nav-link">
                <i class="fas fa-home"></i> Dashboard
            </a>
            <a href="/admin/team/manage" class="nav-link active">
                <i class="fas fa-users-cog"></i> Team Management
            </a>
            <a href="/admin/assign" class="nav-link ">
                <i class="fas fa-plus-circle"></i> Assign Task
            </a>
            <a href="/viewsubadm" class="nav-link">
                <i class="fas fa-user-shield"></i> Sub-admins
            </a>
            <a href="/view-emp" class="nav-link">
                <i class="fas fa-users"></i> Employees
            </a>
            <a href="/admin-subadmin-chat" class="nav-link">
                <i class="fas fa-comments"></i> Chat
            </a>
        </nav>
        <div class="logout-btn">
            <a href="/logout" class="nav-link text-danger" onclick="return confirmLogout()">
                <i class="fas fa-sign-out-alt"></i> Logout
            </a>
        </div>
    </div>

    <main class="main-content">
        <header class="mb-5 d-flex justify-content-between align-items-center">
            <div>
                <h1 class="h3 fw-bold mb-1">Edit Team: <%= team.teamName %>
                </h1>
                <p class="text-muted mb-0">Update team details and coordinate members</p>
            </div>
            <div class="d-flex align-items-center gap-3">
                <button id="themeToggle" class="btn btn-link nav-link p-0" title="Toggle Dark Mode">
                    <i class="fas fa-moon fs-5"></i>
                </button>
                <a href="/admin/team/manage" class="btn btn-secondary px-4 py-2 rounded-pill shadow-sm">
                    <i class="fas fa-arrow-left me-2"></i> Back
                </a>
            </div>
        </header>

        <div class="row g-4">
            <div class="col-lg-8">
                <div class="card list-card p-4">
                    <h5 class="fw-bold mb-4">Update Team Information</h5>
                    <form action="/admin/team/update/<%= team.teamName %>" method="POST">
                        <div class="mb-4">
                            <label for="teamName" class="form-label small fw-bold text-muted text-uppercase">Team
                                Name</label>
                            <input type="text" class="form-control border-0 bg-light" id="teamName" name="newTeamName"
                                value="<%= team.teamName %>" required>
                        </div>
                        <div class="mb-4">
                            <label for="subadminEmail"
                                class="form-label small fw-bold text-muted text-uppercase">Sub-Admin Coordinator</label>
                            <select class="form-select border-0 bg-light" id="subadminEmail" name="subadminEmail"
                                required>
                                <option value="">Select Sub-admin</option>
                                <% subadmins.forEach((subadmin)=> { %>
                                    <option value="<%= subadmin.email %>" <%=subadmin.email===team.subadminEmail
                                        ? 'selected' : '' %>><%= subadmin.email %>
                                    </option>
                                    <% }); %>
                            </select>
                        </div>
                        <div class="mb-4">
                            <label for="employees" class="form-label small fw-bold text-muted text-uppercase">Team
                                Members</label>
                            <select class="form-select border-0 bg-light" id="employees" name="employees" multiple
                                size="10">
                                <% employees.forEach((employee)=> { %>
                                    <% const inCurrentTeam=team.employees.includes(employee.email); %>
                                        <option value="<%= employee.email %>" <%=inCurrentTeam ? 'selected' : '' %>>
                                            <%= employee.email %>
                                                <%= (!inCurrentTeam && employee.teams && employee.teams.length> 0) ? '('
                                                    + employee.teams.join(', ') + ')' : '' %>
                                        </option>
                                        <% }); %>
                            </select>
                            <div class="form-text xsmall">Hold Ctrl/Cmd to toggle member selection.</div>
                        </div>
                        <div class="d-flex gap-3">
                            <button type="submit" class="btn btn-primary px-4 py-2 rounded-pill shadow-sm">
                                <i class="fas fa-save me-2"></i> Save Changes
                            </button>
                            <a href="/admin/team/manage" class="btn btn-light border px-4 py-2 rounded-pill">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="card list-card p-4">
                    <h6 class="fw-bold mb-3">Quick Summary</h6>
                    <div class="mb-3">
                        <label class="small text-muted text-uppercase d-block mb-1">Current Size</label>
                        <span class="h5 fw-bold">
                            <%= team.employees.length %> Members
                        </span>
                    </div>
                    <div class="mb-3">
                        <label class="small text-muted text-uppercase d-block mb-1">Created Date</label>
                        <span class="small">
                            <%= new Date(team.createdAt).toLocaleDateString() %>
                        </span>
                    </div>
                    <hr>
                    <div class="alert alert-info border-0 shadow-sm small py-2 mb-0">
                        <i class="fas fa-info-circle me-1"></i> Changing the team name will automatically update all
                        associated tasks.
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/dark-theme.js"></script>
    <script>
        function confirmLogout() {
            return confirm("Are you sure you want to log out?");
        }
    </script>
</body>

</html>